<%= simple_form_for @item do |f| %>

  <%= f.input :name, required: true, allow_blank: false%>

  <%= f.input :item_cover, as: :file, label: 'Image' %>

  <%= f.input :description %>

  <div class="form-actions">
    <%= f.button :submit, "Create", class: "rounded-lg" %>
  </div>
<% end %>
